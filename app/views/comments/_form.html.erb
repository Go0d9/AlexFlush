<%= form_for ([@tweet, @tweet.comments.build]) do |f| %>
    <p>
      <%= f.label "Автор:" %><br>
      <%= f.text_field :commenter %>
    </p>

    <p>
      <%= f.label "Коментарий:"%><br>
      <%= f.text_area :body, size: "40x6" %>
    </p>
    <%= f.submit "Оставить коментарий", :class=>"button" %>

<% end %>