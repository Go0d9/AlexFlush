<%= form_for @tweet do |f| %>

    <% if @tweet.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@tweet.errors.count, "error") %> prohibited
            this tweet from being saved:
          </h2>
          <ul>
            <% @tweet.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <p>
    <%= select_tag(:category_id, options_for_select(@categories), :prompt => "Select one!") %>      
    </p>
    <p>
      <%= f.label "Название:" %><br>
      <%= f.text_field :title %>
    </p>
 
    <p>
      <%= f.label "Текст:" %><br>
      <%= f.text_area :text %>
    </p>

    <p>
      <%= f.submit "Ввести", :class=>"button" %>
    </p>

<% end %>